version: '3'
services:
  web: &WEB
    environment:
      APP_USER: 'build'
      APP_GROUP: 'build'
    volumes:
      - <%= config.name %>-sync:/app:nocopy

  setup: *WEB

volumes:
  <%= config.name %>-sync:
    external: true
